# Classe Counter: Uma classe da biblioteca `collections` do Python que facilita a contagem de elementos em um iterável, retornando um dicionário onde as chaves são os elementos e os valores são as contagens.

# Dicionário: Estrutura de dados que armazena pares de chave-valor, permitindo acesso eficiente aos valores através de suas chaves, frequentemente usado para mapear e contar ocorrências.

# Enumerate: Função do Python que permite iterar sobre uma lista, retornando tanto o índice quanto o valor de cada elemento, útil para manipulações complexas de dados.

# Mountain Array: Conceito em problemas de arrays que envolve identificar subarrays que formam uma sequência crescente e depois decrescente, semelhante a uma montanha.

# Número inteiro positivo ausente: Problema clássico de encontrar o menor número inteiro positivo que não está presente em um array não classificado, frequentemente resolvido com técnicas de contagem e verificação.

# Strings em arrays: Manipulação de sequências de caracteres dentro de arrays, utilizando técnicas como `enumerate` para acessar índices e valores simultaneamente.

# Técnica de contagem de caracteres: Método eficiente para contar ocorrências de caracteres em uma string, utilizando o método `count` do Python para simplificar o processo.




# Como a função `enumerate` foi utilizada no módulo?: A função `enumerate` foi utilizada para acessar simultaneamente índices e valores em arrays, uma habilidade crucial para resolver problemas que envolvem manipulações complexas de dados. Isso foi especialmente útil em exercícios de manipulação de strings.


# Como o método `count` do Python foi utilizado no módulo?: O método `count` do Python foi utilizado para contar ocorrências de caracteres em uma string. Esta técnica destacou a importância de conhecer e utilizar métodos pré-existentes para tornar o desenvolvimento de software mais eficiente.
